import React from "react";

const Props2 = (props) => {
	return (
		<>
			<div className="container d-flex justify-content-center">
				<button onClick={props.dataa}>Click the Prop2</button>
			</div>
		</>
	);
};

export default Props2;
